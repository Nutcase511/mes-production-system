# Project 字段修复 - 硬刷新说明

## ✅ 代码已修改

我已经将正确的 project 参数直接设置在 Model 组件的 `initialValues` 中，这样在组件初始化时就会使用正确的字段列表。

### 修改内容

**文件**: `src/pages/production/OrderListPageModel.tsx:766-795`

```typescript
export function OrderListPage() {
  const tableConfig = getTableConfig('PRODUCTION_ORDER')
  const tableId = tableConfig?.tableId || '投产通知单'

  // ✅ 直接在初始化时设置正确的 project 参数
  const correctProject = {
    'notificationNumber': 1,
    'orderType': 1,
    'issueDate': 1,
    'planProductionDate': 1,
    'plannedDeliveryDate': 1,
    'customerName': 1,
    'customerOrderNo': 1,
    'orderPriority': 1,
    'creator': 1,
    'auditByOpinion': 1,
    'select-2ECC': 1,
    'receiveBy': 1,
    'id': 1,
    '_id': 1
  }

  return (
    <Model
      name={tableId}
      modelKey="production-orders-list"
      initialValues={{
        wheres: {
          filter: {}
        },
        option: {
          skip: 0,
          limit: 15,
          project: correctProject  // ✅ 直接设置正确的 project
        }
      }}
    >
      <ProductionOrderListContent />
    </Model>
  )
}
```

## 🔧 如何查看更新效果

### 方法1：硬刷新浏览器（推荐）

1. 打开页面 `http://localhost:3000/production/orders`
2. 按 **Ctrl + Shift + R**（Windows）或 **Cmd + Shift + R**（Mac）进行硬刷新
   - 这会清除缓存并重新加载所有资源

### 方法2：清除缓存并刷新

1. 按 **F12** 打开开发者工具
2. 右键点击浏览器的刷新按钮
3. 选择 **"清空缓存并硬性重新加载"**

### 方法3：重启开发服务器

```bash
# 停止当前的开发服务器（Ctrl+C）
# 然后重新启动
npm run dev
```

## 🎯 验证步骤

### 1. 查看控制台日志

打开浏览器控制台 (F12)，应该能看到：

```
✅ Schema 加载成功: {...}
```

### 2. 查看网络请求

1. 打开 **Network** 标签
2. 刷新页面
3. 找到 `投产通知单/d?query=...` 这个请求
4. 查看 **Request Payload** 或 **Query String Parameters**

**应该看到**：
```json
{
  "skip": 0,
  "limit": 15,
  "project": {
    "notificationNumber": 1,
    "orderType": 1,
    "issueDate": 1,
    "planProductionDate": 1,
    "plannedDeliveryDate": 1,
    "customerName": 1,
    "customerOrderNo": 1,
    "orderPriority": 1,
    "creator": 1,
    "auditByOpinion": 1,
    "select-2ECC": 1,
    "receiveBy": 1,
    "id": 1,
    "_id": 1
  },
  "withCount": true
}
```

### 3. 验证返回数据

检查返回的数据是否包含正确的字段：

```javascript
// 在控制台执行
const { items } = useModelList()
console.log('第一条记录:', items[0])
console.log('字段列表:', Object.keys(items[0]))

// 应该包含
// notificationNumber, orderType, issueDate, planProductionDate, plannedDeliveryDate, ...
```

## ❓ 如果还是没有变化

### 检查1：确认代码已更新

查看 `src/pages/production/OrderListPageModel.tsx` 文件的第 780-795 行，确认是否有 `project: correctProject`

### 检查2：确认构建成功

查看终端输出，应该有：
```bash
✓ built in 4.12s
```

### 检查3：清除浏览器缓存

1. 打开开发者工具 (F12)
2. 切换到 **Application** 标签
3. 左侧选择 **Storage** → **Clear site data**
4. 点击 **Clear site data** 按钮
5. 刷新页面

### 检查4：使用无痕模式

1. 打开无痕/隐私浏览窗口
2. 访问 `http://localhost:3000/production/orders`
3. 查看请求参数

## 📊 对比

### 错误的参数（之前）
```json
{
  "project": {
    "notificationNumber": 1,
    "productName": 1,
    "customerName": 1,
    "quantity": 1,
    "deliveryDate": 1,
    "status": 1
  }
}
```

### 正确的参数（现在）
```json
{
  "project": {
    "notificationNumber": 1,
    "orderType": 1,
    "issueDate": 1,
    "planProductionDate": 1,
    "plannedDeliveryDate": 1,
    "customerName": 1,
    "customerOrderNo": 1,
    "orderPriority": 1,
    "creator": 1,
    "auditByOpinion": 1,
    "select-2ECC": 1,
    "receiveBy": 1,
    "id": 1,
    "_id": 1
  }
}
```

## ✅ 构建状态

```bash
✓ built in 4.12s
```

---

**修改时间**: 2026-04-02
**状态**: ✅ 代码已修改，等待硬刷新验证
